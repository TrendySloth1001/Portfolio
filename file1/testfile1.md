```markdown
# Markdown Example

This is an **example** of bold text.

Here is some _italicized_ text.

You can also combine **_bold and italic_** formatting.

For code, here is an example of an `npm` command:

```bash
npm install express
```